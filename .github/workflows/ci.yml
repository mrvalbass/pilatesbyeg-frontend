name: Lint & Tests

on:
  pull_request:
    branches: [dev, main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node setup
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: pnpm install

      - name: Check linter
        run: pnpm lint

      - name: Check Types
        run: pnpm type-check

      - name: Check format
        run: pnpm format:fix

      - name: Run tests
        run: pnpm test
